services:
  - type: web
    name: visual-search
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.8
      - key: PORT
        value: 10000
    autoDeploy: true
